import { VehiclePlateMaskDirective } from './vehicle-plate-mask.directive';

describe('VehiclePlateMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new VehiclePlateMaskDirective();
    expect(directive).toBeTruthy();
  });
});
